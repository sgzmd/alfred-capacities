{
  "name": "alfred-capacities",
  "version": "2.0.0",
  "type": "module",
  "description": "Alfred workflow for Capacities quick capture",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --experimental-strip-types --test 'test/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/remove-markdown": "^0.3.4",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@capacities/api": "^1.1.3",
    "remove-markdown": "^0.7.0"
  }
}
